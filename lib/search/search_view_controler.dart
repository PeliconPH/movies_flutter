import 'package:flutter/material.dart';

class SearchViewControler extends StatefulWidget {
  const SearchViewControler({super.key});

  @override
  State<SearchViewControler> createState() => _SearchViewControlerState();
}

class _SearchViewControlerState extends State<SearchViewControler> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
